<?xml version="1.0" encoding="UTF-8"?>
<arch>
    <family>cortex-m4</family>
    <compile-option>
        <mcpu>cortex-m4</mcpu>
    </compile-option>
    <!--下面是架构中规定由厂家可选实现的部分，创建CPU时，提示用户配置-->
    <vendor-option>
        <fpu-type>fpv4-sp-d16</fpu-type>    <!--默认值，创建新CPU时，需要配置-->
    </vendor-option>
</arch>
<!--
对于F1，不需要vendor-option
对于F3/4/7：有一个vendor-option：fpu-type，创建CPU时，提示用户配置，可选值为“无”或具体浮点处理器（例如fpv4-sp-d16）。
DIDE：若有浮点处理器，则ABI默认设为hard（可改），否则为soft（不可改）；fpu type
-->
